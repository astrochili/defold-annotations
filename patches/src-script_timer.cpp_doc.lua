return {
  ["elements.timer.delay.parameters.callback.types.function(self, handle, time_elapsed)"] = "fun(self, handle:number, time_elapsed:number)",
  ["elements.timer.get_info.returnvalues.data.types.table"] = "{ time_remaining:number, delay:number, repeating:boolean }",
}
