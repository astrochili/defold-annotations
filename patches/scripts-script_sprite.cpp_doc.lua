return {
  ["elements.sprite.play_flipbook.parameters.complete_function.types.function(self, message_id, message, sender)"] = "fun(self, message_id:hash, message:message.sprite.animation_done, sender:url)",
}
